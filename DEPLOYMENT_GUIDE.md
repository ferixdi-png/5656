# Deployment & Testing Guide

## Критерии готовности ✅

Все критерии P0 выполнены:

### 1. Single Instance + No TelegramConflictError ✅
- ✅ Если lock не взят → polling НЕ запускается (только healthcheck)
- ✅ Логи явно показывают когда polling стартует
- ✅ deleteWebhook выполняется перед polling
- ✅ Защита от race condition при deploy/restart на Render

### 2. Полный UX User Flow ✅
- ✅ /start → Главное меню
- ✅ Категории → Модели → Параметры
- ✅ Подтверждение → Генерация → Результат
- ✅ Кнопки "Повторить" / "В меню"
- ✅ Никакой "тишины" - каждая кнопка/ввод получает ответ
- ✅ При ошибке генерации: понятное сообщение + авто-рефанд + retry

### 3. Registry → UI ✅
- ✅ 107 моделей из models/kie_models_source_of_truth.json
- ✅ UI строится из registry
- ✅ Валидация schema для каждой модели

### 4. Payments/Balance ✅
- ✅ PG-backed баланс (в памяти для тестов)
- ✅ Идемпотентность - повторные клики не списывают дважды
- ✅ Резервирование баланса перед генерацией
- ✅ Auto-commit при успехе, auto-refund при ошибке
- ✅ Пользователь видит сообщение о рефанде

### 5. Observability ✅
- ✅ Correlation ID для всех запросов генерации
- ✅ Логи: start, payload, task_id, polling, success/fail, refund
- ✅ Таймауты: 300s для генерации, 120s для smoke test
- ✅ Smoke test режим через ENV

### 6. KIE Integration ✅
- ✅ KIE_BASE_URL без дублирования /api/v1
- ✅ Единый клиент createTask + recordInfo
- ✅ TEST_MODE/KIE_STUB для тестов без сети
- ✅ Progress callback с ETA

---

## Обязательные ENV переменные для Render

```bash
# Обязательные
TELEGRAM_BOT_TOKEN=<your_bot_token>
KIE_API_KEY=<your_kie_api_key>
KIE_BASE_URL=https://api.kie.ai  # без /api/v1 на конце!

# Опциональные (для production)
DATABASE_URL=<postgres_url>  # Для singleton lock и storage
BOT_MODE=polling  # или webhook
PORT=10000  # Для healthcheck

# Для smoke test (опционально)
SMOKE_TEST_ON_START=1
SMOKE_TEST_MODEL_ID=some_model_id
SMOKE_TEST_INPUT_JSON={"prompt":"test"}

# Для тестирования
DRY_RUN=0  # 1 для локальных тестов
TEST_MODE=false
KIE_STUB=false
```

---

## Тестирование (выполнено)

### 1. Компиляция
```bash
python -m compileall .
```
✅ Результат: OK, нет синтаксических ошибок

### 2. Unit тесты
```bash
python -m pytest -q
```
✅ Результат: 36 passed

### 3. Проверка проекта
```bash
python scripts/verify_project.py
```
✅ Результат: OK, 107 models, все инварианты соблюдены

### 4. Startup тест
```bash
python test_bot_startup.py
```
✅ Результат: ALL TESTS PASSED - Bot is ready for deployment

---

## Ручная проверка (5 шагов)

### Шаг 1: Локальный запуск в DRY_RUN
```bash
export DRY_RUN=1
export TELEGRAM_BOT_TOKEN="123456:TEST"
export BOT_MODE=polling
export PORT=0
python main_render.py
```
Ожидаемый результат:
- Лог "DRY_RUN enabled - skipping polling startup"
- Процесс завершается без ошибок

### Шаг 2: Проверить singleton lock (без DATABASE_URL)
```bash
export DRY_RUN=0
export TELEGRAM_BOT_TOKEN="<real_token>"
export BOT_MODE=polling
# НЕ указываем DATABASE_URL
python main_render.py
```
Ожидаемый результат:
- Лог "DRY_RUN enabled - skipping singleton lock" (потому что нет DATABASE_URL)
- deleteWebhook выполнен
- Polling запущен

### Шаг 3: Проверить singleton lock (с DATABASE_URL)
Запустить два процесса параллельно с DATABASE_URL.
Ожидаемый результат:
- Первый: "✅ Singleton lock acquired successfully" → polling запущен
- Второй: "❌ Singleton lock NOT acquired" → polling НЕ запущен, healthcheck живёт

### Шаг 4: Smoke test
```bash
export SMOKE_TEST_ON_START=1
export SMOKE_TEST_MODEL_ID=minimax_video_01
export SMOKE_TEST_INPUT_JSON='{"prompt":"A cat walking"}'
export KIE_STUB=true  # Для теста без реального API
python main_render.py
```
Ожидаемый результат:
- Лог "🧪 Running smoke test"
- Лог "✅ Smoke test PASSED" (в stub mode)
- Polling продолжает запускаться

### Шаг 5: Проверить user flow (с реальным ботом)
1. Запустить бота с реальным TELEGRAM_BOT_TOKEN
2. Отправить /start
3. Выбрать категорию → модель
4. Ввести параметры → подтверждение
5. При успехе: результат + кнопки "Повторить" / "В меню"
6. При ошибке: сообщение об ошибке + "Деньги не списаны" (или "Деньги возвращены")

---

## Как проверить на Render после deploy

1. **Логи при старте:**
   - Проверить что появляется "✅ Singleton lock acquired successfully"
   - Проверить что НЕТ TelegramConflictError
   - Если два инстанса: второй должен показать "❌ Singleton lock NOT acquired"

2. **Health check:**
   - Render должен видеть healthcheck на порту $PORT (10000)
   - Даже если второй инстанс не запустил polling, healthcheck жив

3. **User flow:**
   - Отправить /start боту
   - Проверить что меню открывается
   - Попробовать генерацию (если есть баланс)

4. **Smoke test (если включен):**
   - В логах должен быть результат smoke test
   - Не должен блокировать startup даже при fail

---

## Troubleshooting

### TelegramConflictError всё ещё появляется
- Проверить что DATABASE_URL настроен
- Проверить что оба инстанса могут подключиться к БД
- Проверить логи - второй инстанс должен показать "lock NOT acquired"

### Бот не отвечает на кнопки
- Проверить что zero_silence_router зарегистрирован
- Проверить error_handler_router последним
- Посмотреть логи на предмет исключений

### Генерация зависает
- Проверить KIE_BASE_URL (не должно быть /api/v1 дважды)
- Проверить таймауты (default 300s)
- Посмотреть correlation_id в логах для трассировки

### Деньги списались но генерация failed
- Проверить логи - должен быть "releasing charge (auto-refund)"
- ChargeManager автоматически возвращает при ошибке
- Пользователь должен видеть "Деньги возвращены" или "Деньги не списаны"

---

## Что НЕ включено (out of scope для P0)

- Real PostgreSQL storage integration (используется in-memory для тестов)
- Real payment API integration (заглушки работают)
- История транзакций в UI (ChargeManager готов, UI нет)
- Админ команды (ChargeManager поддерживает, handlers нет)
- Webhook mode (только polling протестирован)
- Multiple language support (только русский)

---

## Следующие шаги (P1)

1. Подключить реальную PostgreSQL для storage
2. Интегрировать реальный payment API
3. Добавить команды /balance, /history
4. Добавить админ команды для управления балансами
5. Расширить smoke test для разных типов моделей
6. Добавить метрики и мониторинг

---

## Итоговый чек-лист перед merge в main

- [x] python -m compileall . - OK
- [x] pytest -q - 36 passed
- [x] python scripts/verify_project.py - OK
- [x] python test_bot_startup.py - OK
- [x] Все P0 требования выполнены
- [x] Документация обновлена
- [ ] Проверен deploy на Render (требует user action)
- [ ] Проверен user flow с реальным ботом (требует user action)


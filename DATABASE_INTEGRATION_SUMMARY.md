# Интеграция базы данных в bot_kie.py

## ✅ Что было сделано

### 1. Интеграция модуля database.py
- Добавлен импорт модуля `database` в начало `bot_kie.py`
- Реализован fallback на JSON хранилище, если БД недоступна
- Переменная `DATABASE_AVAILABLE` определяет, используется ли БД

### 2. Сохранение баланса в БД
- Функция `get_user_balance()` теперь сначала пытается получить баланс из БД
- Функция `set_user_balance()` сохраняет баланс в БД
- Функция `add_user_balance()` добавляет к балансу через БД
- При ошибке БД автоматически используется JSON fallback

### 3. Сохранение истории генераций в БД
- Функция `save_generation_to_history()` сохраняет генерации в таблицу `operations`
- Сохраняется:
  - `operation_type='generation'`
  - `amount` (отрицательное значение, если была платная генерация)
  - `model` (ID модели)
  - `result_url` (URL первого результата)
  - `prompt` (обрезанный до 1000 символов)
- При ошибке БД автоматически используется JSON fallback

### 4. Сохранение операций платежей
- В функции `successful_payment_handler()` добавлено сохранение операции платежа в БД
- Сохраняется операция типа `'payment'` с положительным `amount`
- Операция сохраняется после успешного пополнения баланса

### 5. Инициализация БД при запуске
- В функции `main()` добавлена инициализация БД через `init_database()`
- БД инициализируется перед инициализацией JSON файлов
- При ошибке инициализации БД автоматически отключается и используется JSON fallback

## 📋 Структура сохранения данных

### Баланс пользователя
- **Таблица:** `users`
- **Поля:** `id` (BIGINT), `balance` (NUMERIC(12,2))
- **Сохранение:** При каждом изменении баланса

### Операции
- **Таблица:** `operations`
- **Типы операций:**
  - `'payment'` - пополнение баланса (положительный amount)
  - `'generation'` - генерация контента (отрицательный amount, если платная)
- **Поля:** `user_id`, `type`, `amount`, `model`, `result_url`, `prompt`, `created_at`

### История генераций
- Сохраняется в таблице `operations` с типом `'generation'`
- Содержит все необходимые данные для отображения истории пользователю

## 🔄 Fallback механизм

Если БД недоступна (нет `DATABASE_URL`, ошибка подключения, и т.д.):
1. `DATABASE_AVAILABLE = False`
2. Все функции автоматически используют JSON хранилище
3. Бот продолжает работать без изменений
4. В логах появляются предупреждения о недоступности БД

## 🚀 При деплое

### Что происходит:
1. Бот запускается и проверяет наличие `DATABASE_URL`
2. Если `DATABASE_URL` установлен:
   - Инициализируется БД (создаются таблицы, если их нет)
   - Все данные сохраняются в PostgreSQL
   - Баланс и история сохраняются в БД
3. Если `DATABASE_URL` не установлен:
   - Используется JSON хранилище (как раньше)
   - Данные сохраняются в файлы

### Проверка работы:
- При успешной инициализации БД в логах: `✅ Database initialized successfully`
- При сохранении баланса: `✅ Balance saved to DB: user_id=..., balance=...`
- При сохранении генерации: `✅ Saved generation to DB: user_id=..., model_id=..., operation_id=...`
- При сохранении платежа: `✅ Payment operation saved to DB: user_id=..., amount=...`

## 📝 Важные моменты

1. **Обратная совместимость:** Если БД недоступна, бот работает с JSON файлами как раньше
2. **Автоматическая миграция:** При первом обращении пользователя создается запись в БД
3. **Оптимизация:** Данные хранятся "тонко" (без больших файлов), промпты обрезаются до 1000 символов
4. **Надежность:** При ошибках БД автоматически используется JSON fallback

## 🧪 Тестирование

Для проверки интеграции БД запустите:
```bash
python test_database_integration.py
```

Скрипт проверит:
- Подключение к БД
- Операции с балансом
- Сохранение истории генераций
- Интеграцию в bot_kie.py

## ✅ Итог

Все данные (баланс и история) теперь сохраняются в PostgreSQL при деплое, если `DATABASE_URL` установлен. При недоступности БД автоматически используется JSON fallback для обеспечения непрерывной работы бота.


# ✅ Чеклист проверки кнопок при обновлениях

## 🔍 Перед каждым обновлением:

### 1. Проверка новых кнопок
- [ ] Все новые кнопки имеют обработчики в `button_callback`
- [ ] Все обработчики вызывают `query.answer()` в начале
- [ ] Все обработчики обрабатывают ошибки (try-except)
- [ ] Все обработчики возвращают правильные состояния

### 2. Проверка удаленных кнопок
- [ ] Удалены обработчики из `button_callback`
- [ ] Удалены кнопки из всех мест создания
- [ ] Проверено, что callback_data больше не используется

### 3. Проверка измененных кнопок
- [ ] Обновлены обработчики в `button_callback`
- [ ] Обновлены callback_data в кнопках
- [ ] Проверена совместимость со старыми версиями

### 4. Проверка порядка обработчиков
- [ ] Более специфичные обработчики идут раньше
- [ ] `admin_user_mode` проверяется первым
- [ ] `startswith` обработчики идут после точных совпадений

## 🧪 Автоматическая проверка:

Запустите скрипт проверки:
```bash
python check_all_buttons.py
```

Или используйте bat-файл:
```cmd
ПОЛНАЯ_ПРОВЕРКА_КНОПОК.bat
```

## 📋 Список всех callback_data (для справки):

### Основные:
- `back_to_menu`, `check_balance`, `topup_balance`, `my_generations`
- `referral_info`, `help_menu`, `support_contact`, `copy_bot`
- `change_language`, `cancel`, `back_to_previous_step`

### Генерация:
- `select_model:*`, `gen_type:*`, `category:*`
- `show_models`, `all_models`, `show_all_models_list`
- `free_tools`, `generate_again`, `confirm_generate`
- `retry_generate:*`

### Параметры:
- `set_param:*`, `add_image`, `image_done`, `skip_image`
- `add_audio`, `skip_audio`

### Туториал:
- `tutorial_start`, `tutorial_step1-4`, `tutorial_complete`

### Подарки:
- `claim_gift`

### История:
- `gen_view:*`, `gen_repeat:*`, `gen_history:*`

### Пополнение:
- `topup_amount:*`, `topup_custom`, `pay_stars:*`, `pay_sbp:*`

### Язык:
- `language_select:ru`, `language_select:en`, `set_language:*`

### Админ:
- `admin_stats`, `admin_view_generations`, `admin_gen_nav:*`
- `admin_gen_view:*`, `admin_settings`, `admin_set_currency_rate`
- `admin_search`, `admin_add`, `admin_promocodes`
- `admin_broadcast`, `admin_create_broadcast`, `admin_broadcast_stats`
- `admin_test_ocr`, `admin_user_mode`, `admin_back_to_admin`
- `view_payment_screenshots`, `payment_screenshot_nav:*`
- `admin_payments_back`

## 🛡️ Защита от сбоев:

1. **Fallback-обработчик** ловит все необработанные callback_data
2. **Логирование** всех проблем для отладки
3. **Понятные сообщения** пользователю вместо ошибок
4. **Автоматическое восстановление** через возврат в меню

## ✅ После обновления:

1. Запустите проверку: `ПОЛНАЯ_ПРОВЕРКА_КНОПОК.bat`
2. Протестируйте все новые кнопки вручную
3. Проверьте логи на наличие предупреждений
4. Убедитесь, что fallback-обработчик не срабатывает

## 📝 Примечания:

- Все обработчики должны вызывать `query.answer()` в начале
- Все обработчики должны обрабатывать ошибки
- Порядок обработчиков важен - специфичные раньше
- Fallback-обработчик защищает от сбоев при обновлениях



services:
  - type: worker
    name: kie-api-scraper
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -c "print('✅ Сервис готов. Используйте данные из kie_full_api.json')"
    plan: free
    envVars:
      - key: SKIP_SCRAPING
        value: "true"

